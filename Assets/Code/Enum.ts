export namespace Enum {
    export enum Rotation {
        Front,
        Right,
        Back,
        Left,
    }

    export enum Normal {
        Top,
        Bottom,
        Front,
        Right,
        Back,
        Left,
    }

    export enum Collider {
        Normal,
        OccupancyOnly,
    }

    export enum Sound {
        Interface,
        Machine,
        Interaction,
        Ambient,
    }

    export enum World {
        Industria,
        IndustriaHub,
    }

    export enum Tier {
        StoneAge,
        BronzeAge,
        LowVoltage,
    }
}